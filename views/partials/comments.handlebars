<div class="comments">
    {{#each this}}
    <div>
        <p class="card-body2">
            {{comment_body}} - {{user.username}} {{format_date created_date}}
        </p>
    </div>
    {{/each}}
</div>